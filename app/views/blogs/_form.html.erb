<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :contact %>
    <%= form.text_area :contact %>
  </div>

  <div class="field">
    <%= form.label :write_date %>
    <%= form.text_field :write_date %>
  </div>

  <div class="field">
    <%= form.label :datetime %>
    <%= form.text_field :datetime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
